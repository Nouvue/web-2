[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing: let React Router handle /property, /privacy, /terms,
# /cancellations on direct load or refresh instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
